time.clock() #requires import of time library
<string>.split()

while <expression>: #e.g. while x > 0
	<loop block>
for entry in list: #evaluates each character in string, or each entry in list, sequentially
	<loop block> #runs code for each entry
if <expression>: #e.g. if x > 0
	<block>
else:
	<block>

<string>[index:index]
<list>[index:index]

<search string>.find(<target string>) # returns index of target string within search string

len(<string>)
len(<list>)

eval(code) #evaluates the code

